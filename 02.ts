// achieve cohesion with classes
// contain related code parts (members and functions) in one unit
// as fields and methods in one class

class Point {
    // fields
    x: number;
    y: number;

    //methods
    draw() {
        // ...
    }

    getDistance(another: Point) {
        // ...
    }
}
